# Entradas

- `raw/sell_in_lubrificantes_base.csv`: base principal de trabalho, com os últimos 13 meses e todas as medidas selecionadas.
- `raw/query.sql`: consulta copiada da linhagem do Tableau.
- `archive/sell_in_lubrificantes_historico.csv`: exportação anterior, preservada porque contém um período histórico maior.

Não editar ou sobrescrever esses arquivos. Salvar dados tratados em `outputs/`.
